tickTime={{ zookeeper_ticktime }}
dataDir={{ zookeeper_data_dir }}
clientPort={{ zookeeper_clientPort }}
initLimit={{ zookeeper_initLimit }}
syncLimit={{ zookeeper_syncLimit }}
4lw.commands.whitelist=*
{% for host in groups['zookeeper_servers'] %}
server.{{ loop.index }}={{ hostvars[host].ansible_fqdn }}:{{ zookeeper_leader_port }}:{{ zookeeper_election_port }}
{% endfor %}

